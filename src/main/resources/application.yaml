spring:
  application:
    name: movieflix

  datasource:
    url: jdbc:postgresql://localhost:5430/movieflix
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true